IF( NOT Btparse_FOUND AND ENABLE_BTPARSE )
  ADD_SUBDIRECTORY( btparse )
ENDIF( NOT Btparse_FOUND AND ENABLE_BTPARSE )

IF( NOT Csv_FOUND )
  ADD_SUBDIRECTORY( libcsv )
ENDIF( NOT Csv_FOUND )

ADD_SUBDIRECTORY( rtf2html )
